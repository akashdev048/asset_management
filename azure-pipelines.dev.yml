trigger:
  branches:
    include:
      - dev

resources:
- repo: self

variables:
  imageRepository: 'asset-management-ui'
  containerRegistry: 'assetmanagementdevacr.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and Push Stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: AzureCLI@2
      displayName: Login to ACR, Build and Push Docker Image
      inputs:
        azureSubscription: 'asset-management-dev-service-connection'   # Replace with your RG service connection name
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "Logging into ACR..."
          az acr login --name assetmanagementdevacr.azurecr.io

          echo "Building Docker image..."
          docker build -f $(dockerfilePath) -t $(containerRegistry)/$(imageRepository):$(tag) .

          echo "Pushing Docker image..."
          docker push $(containerRegistry)/$(imageRepository):$(tag)

          echo "Done!"
          

    - task: AzureContainerApps@1
      inputs:
        azureSubscription: 'asset-management-dev-service-connection'
        imageToDeploy: 'assetmanagementdevacr.azurecr.io/asset-management-ui:$(Build.BuildId)'
        containerAppName: 'asset-management-ui-dev-ca'
        resourceGroup: 'asset-management-dev-rg'